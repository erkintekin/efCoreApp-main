<!-- <h1 class="display-1 text-success">Başarılı</h1> -->

@model IEnumerable<KursKayit>

    <h2>Öğrenci Listesi</h2>

    <table class="table table-hover table-dark table-striped table-bordered">
        <thead>
            <tr>
                <th style="width: 150px;">@Html.DisplayNameFor(m => m.KayitId)</th>
                <th>@Html.DisplayNameFor(m => m.Ogrenci.OgrenciAd)</th>
                <th>@Html.DisplayNameFor(m => m.Kurs.Baslik)</th>
                <th style="width: 150px;"></th>
            </tr>
        </thead>
        
        <tbody>
            @foreach(var item in @Model)
            {
                <tr>
                    <td>@item.KayitId</td>
                    <td>@item.Ogrenci.OgrenciAd</td>
                    <td>@item.Kurs.Baslik</td>
                    <td class="text-center">
                        <!-- <form asp-action="Edit" asp-controller="Ogrenci" method="get">
                            <input type="hidden" name="id" value="@item.OgrenciId" /> -->
                            <!-- <button type="submit" class="btn btn-sm btn-primary">Edit</button> -->
                        <a href="/Ogrenci/Edit/@item.OgrenciId" class="btn btn-sm btn-primary">Edit</a>
                    <!-- </form> -->
                        <!-- <form asp-action="Delete" asp-controller="Ogrenci" method="post"> -->
                            <a asp-action="Delete" asp-controller="Ogrenci" asp-route-id="@item.OgrenciId" class="btn btn-danger">Sil</a>
                            <!-- <input type="hidden" name="id" value="@item.OgrenciId" /> -->
                            <!-- <button type="submit" class="btn btn-sm btn-danger">Sil</button> -->
                        <!-- </form> -->
                    </td>
                </tr>
            }
        </tbody>
        </table>
